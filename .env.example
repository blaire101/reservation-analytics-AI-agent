# Secrets only. Copy this file to .env and fill values locally.
OPENAI_API_KEY=
SQL_GATEWAY_USER_ID=
SQL_GATEWAY_TOKEN=

# Optional AWS environment credentials for local development.
# Prefer IAM roles, AWS SSO, or an AWS profile instead of storing keys here.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
