# LlamaIndex + FAISS RAG

The Knowledge path loads a controlled Markdown source, embeds the content, stores vectors in FAISS, retrieves relevant chunks, and produces a grounded answer in real LLM mode.
