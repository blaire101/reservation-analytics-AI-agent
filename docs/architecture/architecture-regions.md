# Region and Cluster Routing

Regional routing happens before SQL execution.

Business context such as country/site is mapped to a region and cluster. The selected environment determines endpoint, cluster, catalog, and physical Data Mart location while the business metric logic remains unchanged.
