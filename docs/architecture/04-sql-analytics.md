# Controlled SQL Analytics

The Analytics path validates context, resolves a unique campaign, selects an allowlisted metric query, executes through QueryBackend, and returns trusted Data Mart numbers.
