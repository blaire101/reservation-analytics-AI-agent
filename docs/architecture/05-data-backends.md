# Serving and Data Backends

FastAPI exposes the agent and Docker packages it. QueryBackend gives the analytics layer one contract while configuration selects SQLite, Athena, or an internal SQL Gateway.
