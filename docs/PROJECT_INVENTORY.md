# Project Inventory

Core application:

- `app/rag/` — RAG pipeline
- `app/analytics/` — validation, resolution, metrics, backends
- `app/graph/` — LangGraph workflow
- `app/api/` — FastAPI
- `app/llm/` — structured LLM client

Business assets:

- `knowledge/` — project knowledge for RAG
- `sample_data/` — deterministic demo data
- `local_data/` — SQLite demo database
- `notebooks/` — learning / walkthrough notebooks
- `presentation/` — synced HTML and PowerPoint
